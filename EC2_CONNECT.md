# EC2 인스턴스 연결 가이드

## 인스턴스 정보
- **인스턴스 ID**: `i-0577bbe710e991e38`
- **키 페어 이름**: `id_rsa`
- **사용자명**: `ec2-user` (Amazon Linux)
- **퍼블릭 DNS**: `ec2-15-134-130-219.ap-southeast-2.compute.amazonaws.com`
- **퍼블릭 IP**: `15.134.130.219`

## 🔑 키 파일 찾기

EC2 인스턴스를 생성할 때 다운로드한 키 파일을 찾아야 합니다.

**가능한 위치:**
- `~/Downloads/id_rsa`
- `~/Downloads/id_rsa.pem`
- `~/Downloads/kimjaeyeon.pem` (찾은 파일)
- 또는 다운로드 폴더의 다른 `.pem` 파일

## 📝 SSH 접속 방법

### 1단계: 키 파일 확인

```bash
# 다운로드 폴더에서 키 파일 찾기
cd ~/Downloads
ls -la *.pem id_rsa 2>/dev/null
```

### 2단계: 키 파일 권한 설정

```bash
# 키 파일 이름을 실제 파일명으로 변경
chmod 400 id_rsa
# 또는
chmod 400 id_rsa.pem
# 또는
chmod 400 kimjaeyeon.pem
```

### 3단계: SSH 접속

**옵션 A: 퍼블릭 DNS 사용**
```bash
ssh -i "id_rsa" ec2-user@ec2-15-134-130-219.ap-southeast-2.compute.amazonaws.com
```

**옵션 B: IP 주소 사용**
```bash
ssh -i "id_rsa" ec2-user@15.134.130.219
```

**키 파일이 다른 이름이면:**
```bash
ssh -i "실제키파일명.pem" ec2-user@15.134.130.219
```

## ⚠️ 주의사항

1. **사용자명 확인**: 
   - Amazon Linux: `ec2-user`
   - Ubuntu: `ubuntu`
   - 화면에 `ec2-user`로 나와 있으므로 Amazon Linux입니다.

2. **키 파일이 없으면**:
   - EC2 콘솔에서 키 페어를 다시 다운로드할 수 없습니다
   - 새 키 페어를 만들고 인스턴스를 재생성해야 합니다
   - 또는 "EC2 Instance Connect" 방법 사용

## 🔄 대안: EC2 Instance Connect 사용

키 파일이 없어도 브라우저에서 직접 접속할 수 있습니다:

1. EC2 콘솔에서 "연결" 버튼 클릭
2. **"EC2 Instance Connect"** 탭 선택
3. **"연결"** 버튼 클릭
4. 브라우저에서 터미널이 열립니다!

이 방법이 가장 간단합니다! 🚀

## 다음 단계 (SSH 접속 후)

```bash
# 1. 시스템 업데이트
sudo yum update -y  # Amazon Linux
# 또는
sudo apt-get update  # Ubuntu인 경우

# 2. Node.js 설치
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs

# 3. 설치 확인
node --version
npm --version
```
